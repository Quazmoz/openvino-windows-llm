# Frontend / pure-logic dev dependencies.
#
# These let you run the server in MOCK mode and exercise the full web UI and
# OpenAI-compatible API on a machine WITHOUT OpenVINO (e.g. macOS / Linux),
# plus run the unit tests for the pure-Python logic.
#   pip install -r requirements-dev.txt

fastapi>=0.110.0
uvicorn[standard]>=0.27.0
pydantic>=2.6.0
python-dotenv>=1.0.0
psutil>=5.9.0

pytest>=8.0.0
httpx>=0.27.0
ruff==0.16.0
